<!DOCTYPE html>
<html>
  <head>
    <title>Siklee - Acesso a plataforma</title>

    <title><%= content_for(:title) || "Siklee" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

     <%= yield :head %>
  </head>

  <body class="h-screen">
    <div class="w-full min-h-screen flex flex-row">
      <div class="w-full bg-white py-10 px-10 lg:w-1/2 flex flex-col space-y-8 lg:space-y-20 flex-none">
        <%= render 'shared/headers/login' %>

        <div class="w-full mx-auto lg:w-3/4">
          <div class="flex flex-col space-y-2 lg:space-y-10">
            <!-- Login header -->
            <h1 class="text-3xl lg:text-4xl font-bold">
              <%= t('pages.login.header.title') %>
            </h1>

            <%= yield %>
          </div>
        </div>
      </div>

      <div style="background-image: url('<%= asset_path('headers/login.jpeg') %>');" class="hidden w-1/2 bg-cover bg-no-repeat bg-center lg:block flex-grow">
        <div class="bg-gradient-to-br from-green-500/30 via-green-600/25 to-green-600/20 w-full h-full relative">
          <div class="max-w-md p-4 py-8 rounded-md absolute bottom-40 left-20 bg-red-100 bg-opacity-20 backdrop-blur-2xl space-y-8">
            <span class="px-5 py-2 bg-green-500 text-base rounded-md flex-shrink-0 max-w-xs">
              Simplificando a Gestão Médica
            </span>

            <p class="text-white">
              Explore a IA que automatiza a gestão dos atestados médicos e tenha tempo para prevenir as faltas no trabalho.
            </p>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
