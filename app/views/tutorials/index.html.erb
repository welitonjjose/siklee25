<h2>Tutoriais</h2>

<p class="description">
  Aqui você encontra tutoriais especiais sobre a nossa plataforma
</p>

<ul class="list-none space-y-4 w-full">
  <% @videos.each do |video| %>
    <li class="max-w-7xl">
      <div class="aspect-w-16 aspect-h-9">
        <iframe
            src="<%= video[:url] %>"
            loading="lazy"
            title="<%= video[:title] %>"
            allow="autoplay;" allowfullscreen
            class="w-full border-2 border-gray-700 p-0 m-0 overflow-hidden"
            >
        </iframe>
      </div>
    </li>
  <% end %>
</ul>
